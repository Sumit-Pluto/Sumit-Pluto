name: Generate Snake

on:
  schedule:
    - cron: "0 */12 * * *" # twice a day
  workflow_dispatch: {} # run manually from the Actions tab
  push:
    branches: [main, master]

permissions:
  contents: write

jobs:
  snake:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Generate contribution snake (light + dark)
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-snake.svg
            dist/github-snake-dark.svg?palette=github-dark

      - name: Publish to the output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

# ─────────────────────────────────────────────────────────────────────────────
# WANT THE ACTUAL PAC-MAN SPRITE (with ghosts) INSTEAD OF THE SNAKE?
# Replace the `snake` job above with this, and point the README image at
# .../output/pacman-contribution-graph.svg . Note: this is a community action
# and can need a manual re-run / tweak; the snake above is the bulletproof option.
#
#   pacman:
#     runs-on: ubuntu-latest
#     permissions: { contents: write }
#     steps:
#       - uses: abozanona/pacman-contribution-graph@main
#         with:
#           github_user_name: ${{ github.repository_owner }}
#           github_token: ${{ secrets.GITHUB_TOKEN }}
#       - run: mkdir -p dist && find . -maxdepth 3 -iname '*pacman*graph*.svg' -exec cp {} dist/ \; || true
#       - uses: crazy-max/ghaction-github-pages@v4
#         with: { target_branch: output, build_dir: dist }
#         env: { GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}" }
# ─────────────────────────────────────────────────────────────────────────────
